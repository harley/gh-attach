# Marketing Assets

This workspace contains the Remotion source for `gh-attach` marketing media.

## Commands

```bash
npm run render:still
npm run render:video
```

Outputs are written to `marketing/renders/` and are gitignored.

## Assets

- `GhAttachStill`: static marketing screenshot
- `GhAttachPromo`: short promo video
